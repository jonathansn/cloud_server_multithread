#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <stdio.h>
#include <string.h>
#include "actions.h"


void exeFunction(char buffer[]);
char** bBuffer(char buffer[]);
char* exe(char **buffer);

#endif